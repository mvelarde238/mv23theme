TODO:

// PRIMERA ETAPA
* check how to handle wrapper components
    - gjs flip box components: fb, fb_front, fb_back
    - gjs accordion replace accordion component
    - gjs wrapper replace wrapper component (ojo, wrapper is body)
    - gjs carousel replace carousel comp (?)

* create php component for: 
    - section / page module
    - fb_front?, fb_back?

* remove unnecesary fields:
    - general: styles
    - row: columns_layout, row_settings (?)
    - accordion: layout in items
    - flip box: layouts, alignment
    
* check css ouput
* save content in all components to test if they are working correctly (file, wp_object, map, icon, etc)
* implement migrator

// SEGUNDA ETAPA
* mambo builder with react
* render frontend on builder (?)